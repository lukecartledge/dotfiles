# Local/private git config
# Copy this file to ~/.gitconfig.local and update with your details
#
# This file is gitignored and should contain machine-specific settings
# like your identity and signing key.
#
# Get your public key with: cat ~/.ssh/id_ed25519.pub

[user]
	name = AUTHORNAME
	email = AUTHOREMAIL
	signingkey = ssh-ed25519 YOUR_PUBLIC_KEY_HERE

[credential]
	helper = GIT_CREDENTIAL_HELPER
