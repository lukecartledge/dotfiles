# Link tmux configuration to home directory
link_home "$HOME_DIR/tmux/tmux.conf" "tmux.conf"

# Status bar theme for tmuxship (see TMUX_SHIP_CONFIG in tmux.conf)
link "$HOME_DIR/tmux/tmuxship.toml" "$HOME/.config/tmux/tmuxship.toml"
